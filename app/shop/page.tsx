import { ProductPurchaseClient } from '../../components/product-purchase-client';

export const metadata = { title: 'NFC Tags & Plans | MyPetID' };

export default function ShopPage() {
  return <main><ProductPurchaseClient /></main>;
}
